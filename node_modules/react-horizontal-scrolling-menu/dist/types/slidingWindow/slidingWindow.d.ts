import type { visibleElements } from '../types';
export declare function prevGroup(allItems: visibleElements, visibleElements: visibleElements): visibleElements;
export declare function nextGroup(allItems: visibleElements, visibleElements: visibleElements): visibleElements;
export declare function slidingWindow(allItems: visibleElements, visibleElements: visibleElements): {
    prev: () => visibleElements;
    next: () => visibleElements;
};
